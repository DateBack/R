#Typeof возвращает тип объекта: int / double, complex... of numeric mode
#Mode возвращает форму(вид) объекта: numeric / logical, character
#Str возвращает краткую информацию об классе и хранящихся в нем данных
